It is possible to restrict domains, meaning to only use the values which result in true

Notation:
![[Pasted image 20240909084326.png]]
Adding a comma after D, adds a rule denoting that only the values that satisfy Q(x) will be used

This also works for existential qualification